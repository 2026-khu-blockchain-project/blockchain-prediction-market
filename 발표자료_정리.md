# PolyPredict — 블록체인 예측시장 dApp 발표 정리

> React + Solidity 기반의 탈중앙 예측시장(Decentralized Prediction Market) dApp
> Polymarket의 핵심 메커니즘을 두 가지 경제 모델로 직접 구현하고, 오라클 자동 정산까지 붙인 프로젝트

---

## 1. 주제 & 문제의식

### 1-1. 주제

**"어떤 미래의 사건이 일어날지(YES/NO)에 USDC를 걸고, 결과가 확정되면 맞춘 사람이 보상을 가져가는 탈중앙 예측시장 dApp"**

- 실제 서비스 **Polymarket**을 벤치마킹
- 예시 시장: `대선 결과`, `챔피언스리그 우승`, `비트코인 $100K 돌파`, `월드컵 16강`, `대한민국 vs 멕시코 경기 결과`

### 1-2. 문제의식 — 왜 "블록체인" 예측시장인가?

기존 중앙화 베팅/예측 서비스는 다음 문제를 가집니다.

| 기존(중앙화) 베팅의 문제 | 블록체인으로 해결 |
| --- | --- |
| 운영사가 정산 결과를 **조작/지연**할 수 있음 | 컨트랙트 코드대로 **자동·강제 실행**, 임의 개입 불가 |
| 운영사가 예치금을 들고 **먹튀(Exit Scam)** 가능 | 예치금이 **컨트랙트(에스크로)** 에 잠겨 있어 코드 외 출금 불가 |
| 배당/확률 산정 과정이 **불투명** | 모든 베팅·정산이 **온체인에 공개**되어 누구나 검증 |
| 본인 자산인데 운영사 **승인 없이는 출금 불가** | 지갑 서명만으로 **본인이 직접** 청구(claim) |

> **핵심 문제의식:** "예측시장에서 가장 중요한 건 **'정산을 믿을 수 있는가'**인데, 이 신뢰를 사람(운영사)이 아니라 **코드와 오라클**에 맡기자."

### 1-3. 이 프로젝트가 활용한 블록체인의 특성 (요약)

- **탈중앙 에스크로** — 담보가 컨트랙트에 잠기고, 정해진 조건에서만 풀림
- **투명성/검증가능성** — 모든 시장·베팅·정산이 온체인 기록
- **자동 실행(스마트 컨트랙트)** — 정산 규칙이 코드로 강제됨
- **토큰화** — YES/NO 포지션을 ERC-1155 토큰으로 발행해 거래·정산
- **오라클** — 외부 현실 데이터(가격·경기결과)를 받아 **사람 없이 자동 정산**

> 상세는 **2번 목차**에서 "어디 코드에서 어떻게 쓰였는지"로 연결.

---

## 2. 활용한 블록체인 특성 (적절성)

> 각 특성이 **"실제 코드 어디에서"** 쓰였는지를 근거로 제시 → 적절성 입증

### 2-1. 탈중앙 에스크로 (Trustless Custody)

사용자가 베팅하면 USDC가 **운영자 지갑이 아니라 컨트랙트 주소**로 들어가고, 컨트랙트는 정산 로직 외의 방법으로는 그 돈을 절대 내보낼 수 없습니다.

- `mintShares()`: `usdc.transferFrom(msg.sender, address(this), _amount)` → 담보가 컨트랙트에 잠김
- 출금 경로는 오직 `burnShares()`(베팅 취소) / `claimWinnings()`(당첨 청구) 두 가지뿐 → **운영자 임의 인출 함수가 존재하지 않음**

### 2-2. 권한 분리 & 자동 실행 (Access Control + Determinism)

- 시장 생성·수동 정산은 `onlyOwner`(관리자)만 가능 → OpenZeppelin `Ownable`
- 하지만 **베팅/청구는 누구나** 가능하고, 청구 금액은 코드가 계산 → 운영자가 "누가 얼마 받을지"를 못 바꿈
- 정산이 끝나면 상태가 `RESOLVED`로 고정되어 **되돌릴 수 없음**(immutable result)

### 2-3. 토큰화 — 포지션을 ERC-1155로 (Tokenization)

YES/NO 포지션 자체를 **표준 토큰**으로 발행 → 지갑에서 잔고 확인 가능, 향후 2차 거래도 가능한 구조.

- 토큰 ID 규칙: `YES = marketId × 2`, `NO = marketId × 2 + 1`

### 2-4. 오라클 — 사람 없는 자동 정산 (Oracle)

블록체인은 "비트코인 가격", "축구 경기 결과"를 스스로 알 수 없음 → **Chainlink 가격 피드 인터페이스(`AggregatorV3Interface`)** 를 통해 외부 데이터를 읽어 자동 정산.

- `resolveMarketWithOracle()`: 마감 후 오라클 값을 읽어 `price >= targetPrice`면 YES, 아니면 NO로 **자동 확정**
- 데모를 위해 `MockV3Aggregator`(가짜 오라클)를 직접 구현 → 관리자가 값을 바꿔 정산 시뮬레이션

### 2-5. 투명성 — 이벤트 로깅 (Transparency)

모든 주요 동작에 `event`를 emit → 블록 익스플로러에서 누구나 추적 가능.

- `MarketCreated`, `SharesMinted`, `MarketResolved`, `WinningsClaimed`, `BetPlaced`, `Claimed`

> **적절성 결론:** 단순히 "DB 대신 블록체인을 썼다"가 아니라, **신뢰가 필요한 지점(담보 보관·정산)에 정확히 블록체인 특성을 배치**했음.

---

## 3. 구현 범위 (Frontend / Backend / Smart Contract)

### 3-0. 아키텍처 한눈에

```
┌────────────────────────────────────────────────┐
│  Frontend (React + Vite + TS)                   │
│  - 시장 목록/상세, 베팅 패널, 관리자 콘솔, 차트     │
│  - wagmi/viem/RainbowKit 로 지갑·컨트랙트 연동     │
└───────────────┬────────────────────────────────┘
                │ 트랜잭션 (approve / mint / bet / resolve / claim)
┌───────────────▼────────────────────────────────┐
│  Smart Contracts (Solidity, on-chain)           │
│  - PolyPredict (CTF/1:1)                         │
│  - PoolBinaryMarket (패리뮤추얼)                  │
│  - MockUSDC / MockV3Aggregator(오라클)            │
└───────────────▲────────────────────────────────┘
                │ 외부 데이터(가격/경기결과)
        ┌───────┴────────┐
        │  Oracle Feed    │
        └─────────────────┘
```

> **"백엔드"의 정의:** 이 프로젝트는 별도 서버(DB/REST API)를 두지 않고, **스마트 컨트랙트가 백엔드 역할**을 합니다. 데이터 저장·비즈니스 로직·인증(지갑 서명)이 모두 온체인에서 처리되는 **풀 Web3 아키텍처**입니다. (배포·테스트는 Hardhat로 수행)

### 3-1. 스마트 컨트랙트 — 직접 개발한 핵심 로직 ⭐

> PPT에서 코드 스니펫으로 직접 보여줄 부분. **두 가지 경제 모델을 직접 설계·구현**한 것이 본 프로젝트의 핵심 기여.

#### (A) CTF 모델 — `PolyPredict.sol`: 1 USDC = YES 1 + NO 1

**1 USDC를 예치하면 YES·NO 토큰을 동시에 발행**하고, 정산 시 **승리 토큰 1개를 1 USDC로 교환**해 주는 구조 (실제 Polymarket의 Conditional Token Framework 방식).

```solidity
// 베팅: USDC 담보를 받고 YES/NO 토큰을 동시 발행
function mintShares(uint256 _marketId, uint256 _amount) external nonReentrant {
    Market storage m = markets[_marketId];
    require(m.state == MarketState.OPEN, "Market is not open");
    require(block.timestamp < m.deadline, "Betting deadline passed");
    require(_amount > 0, "Amount must be > 0");

    // 담보 USDC를 컨트랙트(에스크로)로 이동
    bool success = usdc.transferFrom(msg.sender, address(this), _amount);
    require(success, "USDC transfer failed");
    m.totalCollateral += _amount;

    // 토큰 ID 규칙: YES = id*2, NO = id*2+1
    _mint(msg.sender, _marketId * 2,     _amount, ""); // YES
    _mint(msg.sender, _marketId * 2 + 1, _amount, ""); // NO

    emit SharesMinted(_marketId, msg.sender, _amount);
}
```

```solidity
// 정산: 승리 토큰을 1:1로 USDC 교환, 패배 토큰은 소각
function claimWinnings(uint256 _marketId) external nonReentrant {
    Market storage m = markets[_marketId];
    require(m.state == MarketState.RESOLVED, "Market not resolved yet");

    uint256 yesBalance = balanceOf(msg.sender, _marketId * 2);
    uint256 noBalance  = balanceOf(msg.sender, _marketId * 2 + 1);

    uint256 payout;
    if (m.outcome == Outcome.YES) {
        payout = yesBalance;                       // YES 1개 = 1 USDC
    } else {
        payout = noBalance;
    }
    require(payout > 0, "No winning shares");

    _burn(msg.sender, _marketId * 2,     yesBalance);  // 양쪽 토큰 모두 소각
    _burn(msg.sender, _marketId * 2 + 1, noBalance);

    require(usdc.transfer(msg.sender, payout), "USDC payout failed");
    emit WinningsClaimed(_marketId, msg.sender, payout);
}
```

#### (B) 패리뮤추얼 모델 — `PoolBinaryMarket.sol`: 승자가 전체 판돈을 비율대로 분배

경마/로또 배당처럼 **YES 풀과 NO 풀**을 따로 모으고, **이긴 쪽이 전체 판돈을 자기 지분 비율대로** 나눠 가지는 방식.

```solidity
// 한쪽(YES 또는 NO)에만 베팅. 1인당 1000 USDC·최대 10명 제한(데모)
function placeBet(bool yes, uint256 amount) external nonReentrant {
    require(!resolved, "resolved");
    require(amount > 0, "amount");
    require(yesOf[msg.sender] + noOf[msg.sender] + amount <= MAX_STAKE_PER_USER, "per-user cap 1000");
    if (yes) { require(noOf[msg.sender] == 0, "one side only"); }
    else     { require(yesOf[msg.sender] == 0, "one side only"); }
    // ... 참가자 등록(최대 10명) ...

    usdc.transferFrom(msg.sender, address(this), amount);
    if (yes) { totalYes += amount; yesOf[msg.sender] += amount; }
    else     { totalNo  += amount; noOf[msg.sender]  += amount; }
    emit BetPlaced(msg.sender, yes, amount);
}
```

```solidity
// 정산: 전체 판돈 × (내 지분 / 이긴쪽 풀) 만큼 분배
function claim() external nonReentrant {
    require(resolved, "not resolved");
    uint256 payout;
    if (outcome == Outcome.Yes) {
        uint256 y = yesOf[msg.sender];
        require(y > 0, "no winning YES stake");
        payout = ((totalYes + totalNo) * y) / totalYes;   // ← 변동 배당 핵심 수식
    } else {
        uint256 n = noOf[msg.sender];
        require(n > 0, "no winning NO stake");
        payout = ((totalYes + totalNo) * n) / totalNo;
    }
    yesOf[msg.sender] = 0; noOf[msg.sender] = 0;
    require(usdc.transfer(msg.sender, payout), "payout");
    emit Claimed(msg.sender, payout);
}
```

#### (C) 오라클 자동 정산 — 사람 개입 없이 결과 확정

```solidity
// 마감 후 외부 오라클 값을 읽어 자동으로 승패 확정
function resolveMarketWithOracle(uint256 _marketId) external {
    Market storage m = markets[_marketId];
    require(m.state == MarketState.OPEN, "Market already resolved");
    require(block.timestamp >= m.deadline, "Deadline not reached yet");
    require(m.priceFeed != address(0), "Not an oracle market");

    (, int256 price, , , ) = AggregatorV3Interface(m.priceFeed).latestRoundData();
    Outcome finalOutcome = price >= m.targetPrice ? Outcome.YES : Outcome.NO;

    m.state = MarketState.RESOLVED;
    m.outcome = finalOutcome;
    emit MarketResolved(_marketId, finalOutcome);
}
```

#### 직접 구현한 컨트랙트 목록

| 컨트랙트 | 역할 | 직접 구현 포인트 |
| --- | --- | --- |
| `PolyPredict.sol` | CTF 1:1 정산 예측시장 | ERC-1155 YES/NO 발행, 담보 회계, 수동/오라클 정산 |
| `PoolBinaryMarket.sol` | 패리뮤추얼 풀 베팅 | 풀 회계, **변동 배당 수식**, 인원·금액 제한 |
| `MockUSDC.sol` | 테스트용 6-decimals 담보 토큰 | ERC-20 + free `mint` |
| `MockV3Aggregator.sol` | 오라클 시뮬레이터 | Chainlink 인터페이스 호환, `updateAnswer`로 값 제어 |

#### 안전장치 (보안 고려)

- `ReentrancyGuard` — 재진입 공격 방지 (`mint/burn/claim`)
- `Ownable` — 시장 생성·정산 권한 분리
- `require` 가드 — 마감 시간/상태/잔고/중복청구 검증 (테스트로 검증됨)

### 3-2. 프론트엔드 — Web3 컴포넌트 활용

> "web3 개발에 필요한 컴포넌트를 충분히 활용했는가?"에 대한 근거

| 영역 | 사용 라이브러리 | 활용 내용 |
| --- | --- | --- |
| 지갑 연결 | **RainbowKit** | 메타마스크/WalletConnect 연결 UI |
| 체인 상태/계정 | **wagmi** | `useAccount`, `useReadContract`, `useWriteContract`, `useWaitForTransactionReceipt` |
| 저수준 이더리움 | **viem** | `parseUnits/formatUnits`(6 decimals), `isAddress` |
| 네트워크 가드 | 커스텀 | Polygon Amoy 아니면 전환 배너 표시 |
| 시각화 | **Recharts** | 실시간 배당률/확률 추이 라인차트 |

**핵심: ERC-20 `approve → mint` 2단계 트랜잭션 처리** (모든 토큰 dApp의 표준 패턴을 UI에서 자동화)

```ts
// BettingPanel.tsx — 승인 한도가 부족하면 먼저 approve, 그다음 mintShares
const allowance = allowanceQuery.data ?? 0n;
if (allowance < parsedAmount) {
  const approveHash = await writeContractAsync({
    address: usdcAddress, abi: mockUsdcAbi,
    functionName: 'approve', args: [polyPredictAddress, parsedAmount],
  });
  await waitForTransactionReceipt(config, { hash: approveHash });
}
const mintHash = await writeContractAsync({
  address: polyPredictAddress, abi: polyPredictAbi,
  functionName: 'mintShares', args: [marketId, parsedAmount],
});
await waitForTransactionReceipt(config, { hash: mintHash });
```

추가 구현:
- **온체인 직접 조회** — `useReadContracts`로 모든 시장을 컨트랙트에서 직접 읽음 (별도 백엔드 없이)
- **관리자 콘솔** — `owner` 검증 후에만 시장 생성/정산/오라클 제어 노출
- **연습 모드(데모)** — 지갑·가스 없이 전체 흐름을 시연하는 인메모리 시뮬레이터 (`usePolyPredictDemo`, `usePoolMarketDemo`)

### 3-3. 개발/배포 인프라 (Backend 성격의 도구)

- **Hardhat 3** — 컴파일/테스트/배포 (`deploy.ts` 한 번에 전체 환경 구성)
- **자동 시드** — 배포 시 샘플 시장 6개 생성 + 소유권 사용자 지갑 이전 + `.env` 자동 기록
- **단위 테스트** — `test/PolyPredict.test.ts` (Chai/Mocha): 발행·소각·정산·청구·권한·중복청구 시나리오 검증

---

## 4. 전체 프로젝트 구조 & 구현 목표 달성

### 4-1. 디렉터리 구조

```
blockchain-prediction-market/
├── contracts/                     # 스마트 컨트랙트 (Solidity)
│   ├── PolyPredict.sol            #  - CTF 1:1 예측시장 (핵심)
│   ├── PoolBinaryMarket.sol       #  - 패리뮤추얼 풀 베팅 (핵심)
│   ├── MockUSDC.sol               #  - 테스트 담보 토큰
│   ├── MockV3Aggregator.sol       #  - 오라클 시뮬레이터
│   └── AggregatorV3Interface.sol  #  - Chainlink 오라클 인터페이스
├── scripts/
│   ├── deploy.ts                  # 배포 + 시드 + .env 자동기록
│   ├── seedMarkets.ts / fund_user.ts
├── test/
│   └── PolyPredict.test.ts        # 단위 테스트
├── src/                           # 프론트엔드 (React)
│   ├── pages/                     #  markets / detail / pool / admin / portfolio ...
│   ├── components/                #  BettingPanel / AdminPanel / MarketChart ...
│   ├── hooks/                     #  데모 시뮬레이터 훅
│   ├── contracts/                 #  ABI + 주소 바인딩
│   ├── config/                    #  env.ts(주소·체인) / wagmi.ts(지갑설정)
│   └── lib/market.ts              #  온체인 데이터 → UI 모델 변환
└── hardhat.config.ts
```

### 4-2. 데이터 흐름 (사용자 한 사이클)

```
지갑 연결 → (MockUSDC mint) → 시장 선택 → approve → mint/placeBet
   → 마감 → 관리자 resolve (수동) 또는 resolveWithOracle (자동)
   → 승자 claim → USDC 수령
```

### 4-3. 구현 목표 달성 체크리스트

| 목표 | 상태 | 근거 |
| --- | --- | --- |
| 온체인 담보 예치/정산 | ✅ | `mintShares` / `claimWinnings` |
| 두 가지 경제 모델(CTF·패리뮤추얼) | ✅ | `PolyPredict` + `PoolBinaryMarket` |
| 오라클 기반 자동 정산 | ✅ | `resolveMarketWithOracle`, `resolveWithOracle` |
| 지갑 연동 & 트랜잭션 UX | ✅ | RainbowKit + wagmi, approve→mint 자동화 |
| 관리자 권한 분리 | ✅ | `Ownable` + 프론트 owner 검증 |
| 실시간 확률/배당 시각화 | ✅ | Recharts 라인차트 |
| 발표용 무지갑 데모 모드 | ✅ | `usePolyPredictDemo` / `usePoolMarketDemo` |
| 단위 테스트 | ✅ | `PolyPredict.test.ts` 전 시나리오 |
| 테스트넷 배포 가능 | ✅ | Polygon Amoy / 로컬 Hardhat |

> **결론:** 예측시장의 전체 라이프사이클(시장 생성 → 베팅 → 정산 → 청구)을 **두 모델 + 오라클 + 풀 Web3 프론트**로 빠짐없이 구현해 목표 달성.

---

## 5. 팀원 역할 & 난이도 적절성

> 코드베이스를 기준으로 역할을 분할한 템플릿입니다. **이름 빈칸([팀원 A] 등)에 실제 이름을 넣어** 사용하세요.

### 5-1. 역할 분담 (4명 기준)

| 팀원 | 담당 영역 | 주요 산출물 | 핵심 난이도 |
| --- | --- | --- | --- |
| **[팀원 A]** | 스마트 컨트랙트 (CTF) | `PolyPredict.sol`, ERC-1155 발행/정산 로직, 단위 테스트 | Solidity, 토큰 회계, 재진입 방지 |
| **[팀원 B]** | 스마트 컨트랙트 (패리뮤추얼) + 오라클 | `PoolBinaryMarket.sol`, `MockV3Aggregator`, 자동 정산 | 변동 배당 수식, 오라클 연동 |
| **[팀원 C]** | 프론트엔드 코어 + 지갑 연동 | wagmi/RainbowKit 설정, 베팅 패널, 시장 목록/상세 | Web3 트랜잭션 UX(approve→mint) |
| **[팀원 D]** | 관리자/시각화 + 배포·시연 | 관리자 콘솔, Recharts 차트, `deploy.ts`, 데모 모드 | 배포 파이프라인, 상태 동기화 |

> 5명 이상일 경우: **테스트/QA·문서화** 또는 **UI/UX·디자인(Tailwind)** 담당을 별도 분리하면 자연스럽게 확장됩니다.

### 5-2. 난이도 vs 팀원 수 적절성

본 프로젝트는 **독립적으로 분담 가능한 4개의 큰 기술 축**을 갖습니다.

1. **스마트 컨트랙트 2종** — 서로 다른 경제 모델(고정정산 vs 변동배당) → 각각 한 명이 깊게 맡을 분량
2. **오라클 통합** — 외부 데이터 연동 + Mock 구현 + 자동 정산 검증
3. **Web3 프론트엔드** — 지갑·트랜잭션·온체인 조회·에러 처리
4. **배포/시각화/데모** — Hardhat 배포 자동화 + 실시간 차트 + 무지갑 시연

→ 단순 CRUD 웹앱과 달리 **블록체인 특유의 난이도(가스·서명·재진입·정산 정합성)** 가 있어, **4명 이상이 병렬로 깊게 기여하기에 적절한 규모**입니다. 컨트랙트가 2종이라 "토큰 회계 담당"과 "풀/오라클 담당"을 나눌 수 있고, 프론트도 "거래 흐름"과 "관리자/시각화"로 분리되어 **역할 중복 없이 분담**됩니다.

---

## 부록 A — 실행 방법 (시연 대비)

```bash
# 1. 설치 & 컴파일
npm install
npm run compile

# 2. 로컬 체인 + 배포(샘플 시장 자동 생성)
npx hardhat node            # 별도 터미널
npm run deploy:local

# 3. 프론트 실행
npm run dev

# 테스트
npm test
```

## 부록 B — 핵심 용어 30초 정리

- **CTF (Conditional Token)** — 담보를 내면 YES·NO 토큰을 동시에 받고, 승리 토큰만 1:1로 정산되는 방식 (Polymarket 방식)
- **패리뮤추얼(Parimutuel)** — 이긴 쪽이 전체 판돈을 지분 비율로 나눠 갖는 방식 (경마 배당)
- **오라클(Oracle)** — 블록체인이 모르는 바깥 세상 정보(가격·결과)를 온체인에 넣어주는 다리
- **에스크로(Escrow)** — 조건이 충족될 때까지 자금을 안전하게 잠가두는 장치 (여기선 컨트랙트가 담당)
- **USDC(6 decimals)** — 1 USDC = `1_000_000` 단위. 코드의 `parseUnits(x, 6)` 이유
